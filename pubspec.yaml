name: gestclub_core
description: "GestClub Core - Gerenciador de clubes."
version: 0.0.1
homepage:
resolution: workspace
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  bloc: ^9.0.0
  dartz: ^0.10.1
  dio: ^5.4.0
  equatable: ^2.0.7
  freezed_annotation: ^3.0.0
  get_it: ^8.0.3
  intl: ^0.20.2
  json_annotation: ^4.9.0
  pix_flutter: ^2.2.0
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.9.1
  uuid: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  freezed: ^3.0.6
  json_serializable: ^6.7.1

flutter:
  generate: true
